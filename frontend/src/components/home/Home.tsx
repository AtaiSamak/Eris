import React from "react";
import styles from "../../styles/home/home.module.scss";

const Home = () => {
	return (
		<div className={styles.container}>
			<h1 className={styles.title}>It's React App</h1>
		</div>
	);
};

export default Home;
